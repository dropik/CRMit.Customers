variables:
  - name: buildConfiguration
    value: Debug

  - name: projectName
    value: CRMit.Customers


jobs:
  - job: unittest
    displayName: Unit Testing
    steps:
      - task: UseDotNet@2
        inputs:
          version: '5.0.x'

      - task: DotNetCoreCLI@2
        inputs:
          command: 'test'
          arguments: --configuration $(buildConfiguration) --results-directory TestResults --collect:"XPlat Code Coverage" --settings unittest.runsettings
          publishTestResults: true
          projects: 'test/unittest/$(projectName).UnitTests/$(projectName).UnitTests.csproj'

      - task: PublishCodeCoverageResults@1
        displayName: 'Publish code coverage report'
        inputs:
          codeCoverageTool: 'Cobertura'
          summaryFileLocation: '$(Build.SourcesDirectory)/TestResults/**/coverage.cobertura.xml'
          